table! {
    users (id) {
        id -> Varchar,
        username -> Varchar,
        password -> Varchar,
        role -> Varchar,
        created_by -> Varchar,
        created_at -> Timestamp,
        updated_by -> Varchar,
        updated_at -> Timestamp,
    }
}
